from pydantic import BaseModel, Field
from typing import Dict

class AnalyzeMarketDataParameters(BaseModel):
    dex_data: Dict[str, str] = Field(
        description="DexScreener data containing market metrics"
    )
